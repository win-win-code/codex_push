# Use this only when ~/.codex/config.toml does not already define top-level notify.
# Keep notify at the top level (before any [table] that follows it).
# If notify already exists, preserve it and add telegram_notify.py to its fanout.
notify = ["/usr/bin/python3", "/Users/USERNAME/.codex/telegram_notify.py"]
